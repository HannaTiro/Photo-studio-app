<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:vm="clr-namespace:PhotoStudio.MobileApp.ViewModels"
             mc:Ignorable="d"
             x:Class="PhotoStudio.MobileApp.Views.LoginPage"
             Shell.NavBarIsVisible="False"
             BackgroundColor="RosyBrown">
    <ContentPage.BindingContext>
        <vm:LoginViewModel></vm:LoginViewModel>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout Padding="100" VerticalOptions="CenterAndExpand" HorizontalOptions="FillAndExpand">
            <Label FontSize="Large" Text="Login" HorizontalOptions="CenterAndExpand" VerticalOptions="Center"></Label>
            <Label Text="Username" HorizontalOptions="CenterAndExpand"></Label>
            <Entry Text="{Binding Username}"></Entry>
            
            <Label Text="Password" HorizontalOptions="CenterAndExpand"></Label>
            <Entry Text="{Binding Password}"></Entry>
            
            <Button VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}" />

           
        </StackLayout>
    </ContentPage.Content>
</ContentPage>